<div class="content_post">
  <div class="main_title">
    <caption><%= blog.title %></caption>
  </div>
    <% if user_signed_in? && current_user.id == blog.user_id %>
      <div class="more">
        <ul class="more_list">
          <li class="box1">
            <%= link_to '編集', "/blogs/#{blog.id}/edit", method: :get, style: "color:white;" %>
          </li>
          <li class="box2">
            <%= link_to '削除', "/blogs/#{blog.id}", method: :delete, style: "color:white;" %>
          </li>
        </ul>
      </div>
    <% end %>
    <%= simple_format(blog.text) %>
    <span class="name">
        <span>投稿者 :</span><%= blog.user.nickname %>
      </a>
    </span>
</div>
