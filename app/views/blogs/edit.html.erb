<div class="contents_row">
    <%= form_with( url:"/blogs/#{@blog.id}", method: :patch, local:true ) do |form| %>
      <h3>
        編集する
      </h3>
      <textarea class="edit_box" cols="30" name="text" placeholder="text" rows="10"><%= @blog.text %></textarea>
      <input type="submit" value="SENT">
    <% end %>
  </div>
